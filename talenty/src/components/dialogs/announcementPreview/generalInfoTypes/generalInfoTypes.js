import Title from "./Title";
import GridSection from "./GridSection";
import Section from "./Section";

export const generalInfoTypes = {
    title: Title,
    grid_section: GridSection,
    section: Section,
}